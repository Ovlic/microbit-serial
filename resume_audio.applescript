
-- "System Events" is the application responsible for keystrokes, mouse clicks, etc.

-- Tell "System Events" to do everything between it and "end tell"
tell application "System Events"
    
        -- Keystroke for "Youtube Music Desktop" app
        keystroke space using {command down, shift down}

        -- Keystroke for "Youtube" (In browser)
        -- keystroke "k"

-- End telling "System Events" to do stuff
end tell
